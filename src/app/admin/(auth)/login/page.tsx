import { LoginForm } from "@/features/auth/admin/components/loginForm";

const Login = () => {
  return (
    <section className="max-w-xl mx-auto min-h-svh items-center justify-center flex">
      <LoginForm className="w-full" />
    </section>
  );
};

export default Login;
